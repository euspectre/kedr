# Header
header = A bit more of a comment
footer = Closing remarks

[group]
	function.name = firstFunction

	arg.type = char
	arg.name = c

	arg.type = pid_t
	arg.name = pid

[group]
	function.name = secondFunction

[group]
	function.name = thirdFunction

	arg.type = rise_t
	arg.name = highRise

	arg.type = work_t
	arg.name = work

	arg.type = bool
	arg.name = bOK
