user space библиотека использует библиотеку syscall_simulator, поэтому для успешной компиляции и запуска библиотеки, вначале makefile'а необходимо заменить значений переменной SYSCALL_CONNECTOR_LIBRARY_PATH на путь к директории, в которой находится библиотека syscall_connector.

В поддиректории example находится пример по использованию библиотеки. Он устанавливает индикатор для точки симуляции kmalloc в fault_if_size_greater - если размер, с которым вызвана симуляция для точки, превосходит размер, переданный при установке индикаторной функции, то генерируется ошибка(индикатор возвращает не 0).