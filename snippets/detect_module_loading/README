Пример отслеживания загрузки/выгрузки модулей.
Например, для внедрения в их исполнение.

1. Возможность фильтровать модули, за которыми надо следить (функция filter_module).
2. Возможность установить функцию, которая будет вызвана при загрузке интересуемого модуля (on_module_load).
3. Возможность установить функцию, которая будет вызвана при выгрузке модуля, за которым следим (on_module_unload).
4. Возможность установить функцию, которая будет вызвана для всех модулей, за которыми следим, в случае выгрузки самого loading_detector'а (on_detector_unload).


