Примеры для проведения "перехвата вызовов вручную" (под системой Crash).

Вызовы __kmalloc из модуля icpt_target вручную заменяются в runtime на 
вызовы replacement-функций из модуля icpt_watcher. 

Одна replacement-функция просто выводит аргументы __kmalloc в лог, другая 
просто возвращает NULL (failure simulation).

В каталоге test - программа, которая обращается к модулю icpt_target и 
дёргает нужные операции в нём.
