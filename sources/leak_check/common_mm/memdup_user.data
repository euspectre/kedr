[group]
	# The body of the replacement function
	handler.post =>>
	if (!IS_ERR(returnValue))
		kedr_lc_handle_alloc(target_module, returnValue, len,
			caller_address);
	<<
#######################################################################
