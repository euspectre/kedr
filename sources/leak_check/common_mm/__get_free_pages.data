[group]
	# The body of the replacement function
	handler.post =>>
	if ((void *)returnValue != NULL) {
		kedr_lc_handle_alloc(target_module, (const void *)returnValue, 
			(size_t)(PAGE_SIZE << order), caller_address);
	}
	<<
#######################################################################
