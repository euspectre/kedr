[group]
	# The body of the replacement function
	handler.post =>>
	if (ret_val != NULL)
		kedr_lc_handle_alloc(target_module, (void *)ret_val, 
			strnlen(s, max) + 1, caller_address);
	<<
#######################################################################
