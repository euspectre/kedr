[group]
	# The body of the replacement function
	handler.pre =>>
	if (!ZERO_OR_NULL_PTR(addr))
		kedr_lc_handle_free(addr, 
			caller_address);
	<<
#######################################################################
