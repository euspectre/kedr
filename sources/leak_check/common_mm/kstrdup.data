[group]
	# The body of the replacement function
	handler.post =>>
	if (returnValue != NULL)
		kedr_lc_handle_alloc(target_module, (void *)returnValue, 
			strlen(s) + 1, caller_address);
	<<
#######################################################################
