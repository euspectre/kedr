[group]
	# Name and return type of the target function
	function.name = vmalloc_32
	returnType = void *

	# Names and types of the arguments of the target function
	arg.type = unsigned long
	arg.name = size
	
	# The body of the replacement function
	handler.post =>>
	if (returnValue != NULL)
		klc_add_alloc(returnValue, size, stack_depth);
	<<

# End of the group of definitions for vmalloc_32().

#######################################################################
