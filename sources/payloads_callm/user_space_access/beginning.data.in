# This module processes the calls to functions for access to user space.

module.name = @kmodule_name@
module.author = Tsyvarev
module.license = GPL

header =>>
#include <linux/uaccess.h>   /*  user space access functions */
<<
