[group]
	# Name and return type of the target function
	function.name = __init_waitqueue_head
	returnsVoid = yes

	# Names and types of the arguments of the target function
	arg.type = wait_queue_head_t*
	arg.name = q

	arg.type = struct lock_class_key*
	arg.name = key

	# The parameters to be output to the trace. 
	trace.param.type = wait_queue_head_t*
	trace.param.name = q

	trace.param.type = struct lock_class_key*
	trace.param.name = key

	# The format string to be used for trace output.
	trace.formatString = "arguments: (%p, %p)"

# End of the group of definitions for __init_waitqueue_head().

#######################################################################
