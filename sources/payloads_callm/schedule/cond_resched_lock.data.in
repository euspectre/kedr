[group]
	# Name and return type of the target function
	function.name = @cond_resched_lock_FUNCTION@
	returnType = int

	# Names and types of the arguments of the target function
	arg.type = spinlock_t*
	arg.name = lock

	# The parameters to be output to the trace. 	
	trace.param.type = spinlock_t*
	trace.param.name = lock

	trace.param.type = int
	trace.param.name = returnValue

	# The format string to be used for trace output.
	trace.formatString = "arguments: (%p), returns: %d"

# End of the group of definitions for @cond_resched_lock_FUNCTION@().

#######################################################################
