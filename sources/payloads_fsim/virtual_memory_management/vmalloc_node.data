[group]
	# Name and return type of the target function
	function.name = vmalloc_node
	returnType = void*

	# Names and types of the arguments of the target function
	arg.type = unsigned long
	arg.name = size
	
	arg.type = int
	arg.name = node

	# Fault Simulation
	fpoint.reuse_point = vmalloc
	fpoint.fault_code =	returnValue = NULL;

# End of the group of definitions for vmalloc_node().
#######################################################################
