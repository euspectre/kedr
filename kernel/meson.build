# We only install the sources of the kernel-mode parts of KEDR here.
install_subdir('arch', install_dir : kedr_kmod_srcdir)
install_subdir('include', install_dir : kedr_kmod_srcdir)
install_subdir('kernel', install_dir : kedr_kmod_srcdir)
install_data('Makefile', install_dir : kedr_kmod_srcdir)
