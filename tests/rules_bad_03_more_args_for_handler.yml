# Bad rule: more arguments than expected are passed to the handler, leaving
# no place for kedr_local*.
############################################################################

# kedr_handle_alloc() has 3 parameters: 2 are available to the rules +
# kedr_local* is passed automatically.
__kmalloc:
  post: |
    kedr_handle_alloc(ret, arg1, 0)
############################################################################
