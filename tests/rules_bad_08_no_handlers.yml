# Bad rule: it is not empty but calls only helpers, no handler calls.
############################################################################

kfree:
  pre: |
    kedr_size_from_order(0)
############################################################################
